1. first import the following componets for app bar
import AppBar from '@material-ui/core/AppBar';
import Tabs from '@material-ui/core/Tabs';
import Tab from '@material-ui/core/Tab';
import Toolbar from '@material-ui/core/Toolbar';
import React from 'react';


2.now inside export default function create React.Fragment for main div inside it then use the following patter   Appbar/Toolbar/Tabs/Tab


3.now if app bar is displayed and we want for its customization then we will use makestyle will not go for the theming system just custom style in theming overview 

                         import { createMuiTheme, makeStyles, ThemeProvider } from '@material-ui/core/styles';

now outside the export default use the custome style without theme

                             const useStyles = makeStyles({  });

now use a hoook for it  inside export default 
              
	                const classes = useStyles();
now we are ready to go 

const useStyles = makeStyles({
    Appbar:{
        //here is the customization of app bar
    // backgroundColor:"red",
    // marginTop:'8em',
    // borderRadius:'5em',
},
Tabs:{
    // borderRadius:'5em',
    // backgroundColor:"red",
},
Tab:{
   TextColor:"red",
   backgroundColor:"red",
   borderRadius:'5em',
   
   note for hovering using the hover function inside the function like tabs:{    '&:hover':{ backgroundColor:"yellow",}     }
   '&:hover':{
    backgroundColor:"yellow",
    color:'pink', 
   },
}

});




<AppBar    className={classes.Appbar}>
    <toolbar>
    <Tabs  aria-label="simple tabs example"  className={classes.Tabs}>
    <Tab label="Item One" className={classes.Tab} />
    <Tab label="Item Two" />
    <Tab label="Item Three" />
  </Tabs>
    </toolbar>
</AppBar>


